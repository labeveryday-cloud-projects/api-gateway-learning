// Customize the look and feel of your app using Material-UI's theming.
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#007AFF', // Adjust to your preferred primary color
    },
    secondary: {
      main: '#FF9500', // Adjust to your preferred secondary color
    },
  },
  typography: {
    // Customize typography here
  },
});

export default theme;
